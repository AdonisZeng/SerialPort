# 接收时间戳显示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在"16进制发送"左侧新增"时间戳显示"复选框，勾选后接收窗口每行数据行首显示收到时刻 `[HH:mm:ss]`，保存文件同步带时间戳。

**Architecture:** MainWindow 层跨块状态机（`_atLineStart` + `_lastWasCr`）对每次接收的增量文本做行首补戳变换；状态机始终运行、仅勾选时输出时间戳；变换后的文本统一流向下游（显示 / 筛选缓冲 / 保存文件），不触碰服务层。

**Tech Stack:** .NET Framework 4.8.1、WPF、旧式（非 SDK）MSBuild 工程、无 NuGet 依赖。

## Global Constraints

- 构建必须用 VS 的 MSBuild.exe（dotnet CLI 无法编译旧式 WPF 标记）：
  `"/d/Software/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" SerialPort.csproj`
- **构建完成后不运行程序**——由开发者自行运行验证效果。
- 所有注释与界面字符串为中文。
- 不整体重赋 `txtReceive.Text`（保持增量 AppendText 性能模式）。
- 时间戳格式：`[HH:mm:ss]`（时:分:秒，无日期、无毫秒）。
- 行以 `\n`、`\r`、`\r\n` 终结（含跨块拆分的 `\r`+开头`\n`）；空行不加戳。
- 复选框放置位置："打开串口"按钮（`btnToggleConnect`）与"16进制发送"（`chkSendHex`）之间。
- 状态机（行首位置跟踪）必须**始终运行**，与复选框勾选状态无关；仅当勾选时输出时间戳前缀。

---
### Task 1: 新增"时间戳显示"复选框

**Files:**
- Modify: `MainWindow.xaml:122`（配置区行2 StackPanel，`chkSendHex` 之前）

**Interfaces:**
- Produces: `chkTimestamp`（`CheckBox`，控件字段由 XAML 编译器生成，供 Task 2 读取 `IsChecked`）

- [ ] **Step 1: 插入复选框**

把：

```xml
                    <CheckBox x:Name="chkSendHex" Content="16进制发送" Margin="0,0,18,0" VerticalAlignment="Center" />
```

改为（前面插入一行）：

```xml
                    <CheckBox x:Name="chkTimestamp" Content="时间戳显示" Margin="0,0,18,0" VerticalAlignment="Center" />
                    <CheckBox x:Name="chkSendHex" Content="16进制发送" Margin="0,0,18,0" VerticalAlignment="Center" />
```

- [ ] **Step 2: 构建验证**

Run: `"/d/Software/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" SerialPort.csproj -v:m -nologo`
Expected: `SerialPort -> D:\Development\C#\SerialPort\bin\Debug\SerialPort.exe`，无警告。

- [ ] **Step 3: 提交**

```bash
git add MainWindow.xaml
git commit -m "feat: 新增时间戳显示复选框"
```

---
### Task 2: 行首时间戳状态机并接入数据流

**Files:**
- Modify: `MainWindow.xaml.cs`（字段区 `:23` 附近、`GetIncrementText` 之后约 `:249`、`OnDataReceived` 的 `:216`）

**Interfaces:**
- Consumes: `chkTimestamp.IsChecked`（Task 1 生成的复选框）；`GetIncrementText(byte[])`（现有方法，返回单次接收的显示文本）
- Produces: `string GetTimestampedText(string increment)` —— 行首补 `[HH:mm:ss] ` 后的文本；状态字段 `_atLineStart`、`_lastWasCr`（跨调用保持）

- [ ] **Step 1: 添加状态字段**

在 `MainWindow.xaml.cs` 的筛选状态字段（`// 筛选状态` 注释块）之后追加：

```csharp
        // 时间戳显示状态（状态机始终运行，与复选框勾选状态无关）
        private bool _atLineStart = true;   // 当前处于行首（下一个非换行字符应补时间戳）
        private bool _lastWasCr;            // 上一字符是 \r（用于区分 \r\n 与单独 \r，含跨块拆分）
```

- [ ] **Step 2: 实现 `GetTimestampedText`**

在 `GetIncrementText` 方法定义之后追加：

```csharp
        /// <summary>
        /// 给单次接收的增量文本补时间戳：每行行首加 [HH:mm:ss]。
        /// 行以 \n、\r、\r\n（含跨块拆分的 \r+ 开头\n）终结；空行不加戳；
        /// 仅当勾选"时间戳显示"时输出前缀，行首位置跟踪始终进行。
        /// </summary>
        private string GetTimestampedText(string increment)
        {
            StringBuilder sb = new StringBuilder(increment.Length + 12);
            bool emitTimestamp = chkTimestamp.IsChecked == true;
            bool atLineStart = _atLineStart;
            bool lastWasCr = _lastWasCr;
            foreach (char c in increment)
            {
                // 先判定上个字符：单独 \r 在此字符处终结上一行（须先于补戳检查，\r 后首个字符才能正确加戳）
                if (lastWasCr)
                {
                    if (c != '\n') atLineStart = true;
                    lastWasCr = false;
                }
                if (atLineStart && emitTimestamp && c != '\r' && c != '\n')
                {
                    sb.Append('[').Append(DateTime.Now.ToString("HH:mm:ss")).Append("] ");
                    atLineStart = false;
                }
                sb.Append(c);
                if (c == '\r') lastWasCr = true;         // 等待可能的 \n
                else if (c == '\n') atLineStart = true;  // \n（或 \r\n 的 \n）终结一行
            }
            _atLineStart = atLineStart;
            _lastWasCr = lastWasCr;
            return sb.ToString();
        }
```

- [ ] **Step 3: 接入 `OnDataReceived`**

把（`MainWindow.xaml.cs:216`）：

```csharp
            string increment = GetIncrementText(e.Data);
```

改为：

```csharp
            string increment = GetTimestampedText(GetIncrementText(e.Data));
```

其余路由（显示 / `_filterState.Buffer` / `_saveFileWriter`）不动——它们拿到的已统一是带时间戳文本。

- [ ] **Step 4: 构建验证**

Run: `"/d/Software/Microsoft Visual Studio/18/Community/MSBuild/Current/Bin/MSBuild.exe" SerialPort.csproj -v:m -nologo`
Expected: `SerialPort -> D:\Development\C#\SerialPort\bin\Debug\SerialPort.exe`，无警告。

- [ ] **Step 5: 提交**

```bash
git add MainWindow.xaml.cs
git commit -m "feat: 接收区行首显示时间戳（勾选时间戳显示时）"
```

---
### Task 3: 开发者运行验证（移交人工）

本计划不含自动化测试（项目无测试框架）。按项目规则构建完成后由开发者运行，验证清单（来自设计文档）：

1. 勾选"时间戳显示"后，文本模式下整行 / 半行（无换行结尾）/ 跨块拆分（含 `\r` 与 `\n` 分属两次接收）数据的行首时间戳正确；
2. 十六进制显示模式下每块一个时间戳；
3. 空行（连续 `\r\n`）不产生时间戳；
4. 中途勾选 / 取消勾选：当前未结束行不补戳，下一行恢复；
5. "文件保存"写出内容与窗口一致（均带时间戳）；
6. 筛选启用时行为正常，未勾选时间戳时显示与之前完全一致。
